import ChatField from './components/chatField';

export const metadata = {
  title: {
    absolute: "MulyonoGPT - Pria Solo AI",
  },
};

const Home = async () => {
  return (
        <ChatField />
  );
};

export default Home;
